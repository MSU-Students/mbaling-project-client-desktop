import { MutationTree } from 'vuex';
import { LandlordStateInterface } from './state';

const mutation: MutationTree<LandlordStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
};

export default mutation;

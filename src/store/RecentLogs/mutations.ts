import { MutationTree } from 'vuex';
import { RecentStateInterface } from './state';

const mutation: MutationTree<RecentStateInterface> = {
  someMutation (/* state: ExampleStateInterface */) {
    // your code
  }
};

export default mutation;
